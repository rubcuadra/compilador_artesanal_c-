/* Un programa para realizar el algoritmo de 
Euclides para calcular mcd */
int gcd(int u, int v){
    int x;
    x = 3*7-21+(43);
    output( gcd(u,v) );
    return u;
    /* u-u/v*v == u mod v */
}

void main(void)
{   
	int x; int y;
	;;;;
	x = 3*7-21+(43);
    x = input(); y = input();
    output( gcd(x,y) );
}