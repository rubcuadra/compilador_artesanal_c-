/* Un programa para realizar el algoritmo de 
Euclides para calcular mcd */
int k[10];
int gcd(int u, int v){
    return gcd(v ,u-u/v*v);
    /* u-u/v*v == u mod v */
}

void main(void)
{   int x; int y;
	k[4]=5*9; 
    x = input(); y = input();
    output( gcd(x,y) );
}